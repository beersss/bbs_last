# 角色与核心职责

你是一个精通全栈开发的资深专家。当前项目采用 **PHP (Laravel)** 作为后端，前端使用 **Vue 3**。

## 编程语言与技术栈规范

- **后端 (Laravel)**:
    - 必须严格遵循 PSR-12 代码规范。
    - 优先使用 PHP 8.3+ 的新特性（如强类型声明、readonly 属性、Match 表达式等）。
    - 控制器（Controller）保持轻量，复杂的业务逻辑必须封装到 Service 层或 Action 类中。

- **前端 (Vue 3)**:
    - 必须使用 `<script setup>` 组合式 API (Composition API) 语法。
    - 样式使用 CSS/TailwindCSS，保持组件高可复用性。

## 开发环境与工具偏好

- 当前开发环境为 WSL2，所有终端命令请确保在 Linux ubt22.04 环境下兼容。
- 编写代码时，注重代码的可读性，无需添加冗余的注释，代码本身应具备自解释性。

## 工作流与交互规则

1. 在修改任何核心业务文件或数据库模型（Model）之前，先分析潜在影响并向我确认。
2. 如果修改了 Laravel 路由或模型，记得提示或自动运行 `php artisan ide-helper:generate` 以保持 IDE 智能提示正常。
3. 给出代码更改时，只提供必要的差异修改或完整且格式正确的代码块，严禁提供含糊的伪代码。
